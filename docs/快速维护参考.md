# 快速维护参考

## 🚀 一键操作

### 打开 NPM 管理界面

**方法1：使用 GUI**
```bash
python3 service-manager.py
# 点击 "🌐 打開 NPM 管理界面" 按钮
```

**方法2：直接访问**
```
http://192.168.10.1:81
```

### 检查证书状态

```bash
./check-ssl-cert.sh
```

### 打开 GUI 服务管理器

```bash
python3 service-manager.py
```

## 📋 常用检查命令

```bash
# 检查网站访问
curl -I https://icthelper.duckdns.org

# 检查证书有效期
echo | openssl s_client -servername icthelper.duckdns.org -connect icthelper.duckdns.org:443 2>/dev/null | openssl x509 -noout -dates

# 检查后端服务
ps aux | grep "python.*app.py"

# 检查 NPM 容器
sudo docker ps | grep npm

# 检查防火墙
sudo ufw status
```

## 🔄 证书更新流程（简化版）

1. **检查证书是否续期**
   ```bash
   ./check-ssl-cert.sh
   ```

2. **获取新证书**
   ```bash
   cat ~/.acme.sh/icthelper.duckdns.org_ecc/icthelper.duckdns.org.cer
   cat ~/.acme.sh/icthelper.duckdns.org_ecc/icthelper.duckdns.org.key
   ```

3. **更新到 NPM**
   - 打开 NPM：`http://192.168.10.1:81`
   - SSL Certificates → Edit → 粘贴新证书 → Save

4. **验证**
   ```bash
   curl -I https://icthelper.duckdns.org
   ```

## 📞 紧急情况

### 网站无法访问

1. 检查后端：`ps aux | grep "python.*app.py"`
2. 检查 NPM：`sudo docker ps | grep npm`
3. 检查防火墙：`sudo ufw status`
4. 查看日志：`sudo docker logs npm`

### 证书过期

1. 手动续期：`acme.sh --renew -d icthelper.duckdns.org --force`
2. 更新到 NPM（见上方流程）
3. 验证访问

## 📅 维护提醒

- **每月**：运行 `./check-ssl-cert.sh`
- **每 60-90 天**：更新证书到 NPM
- **每 80 天**：设置日历提醒检查证书

