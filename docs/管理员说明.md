# 🔐 管理員系統說明

## 📍 用戶數據存儲位置

所有用戶數據都存儲在瀏覽器的 **localStorage** 中：

### 存儲的數據鍵：

1. **`sql-users`** - 所有用戶賬戶信息
   - 包含：用戶名、密碼、電郵、註冊時間等

2. **`user-progress-{username}`** - 每個用戶的學習進度
   - 包含：完成的練習、分數、學習時長、成就等

3. **`user-profile-{username}`** - 每個用戶的個人資料
   - 包含：用戶名、電郵等

4. **`current-user`** - 當前登錄的用戶名

## 👤 管理員設置

### 默認管理員賬號

- **用戶名**：`YIVH`
- **默認密碼**：`admin123`（建議首次登錄後修改）
- **電郵**：`admin@sql-training.com`

### 管理員權限

管理員（YIVH）可以：
- ✅ 訪問管理員面板（`admin.html`）
- ✅ 修改API密鑰（通過網頁界面）
- ✅ 查看所有用戶
- ✅ 刪除用戶（不能刪除自己）
- ✅ 查看用戶詳細信息
- ✅ 導出/備份所有數據
- ✅ 清空所有數據（危險操作）
- ✅ 查看系統統計

## 🚀 使用管理員功能

### 步驟1：登錄管理員賬號

1. 在任何頁面點擊"登錄"
2. 輸入用戶名：`YIVH`
3. 輸入密碼：`admin123`（或您修改後的密碼）
4. 點擊登錄

### 步驟2：訪問管理員面板

登錄後，導航欄會顯示"🔐 管理員"鏈接，點擊即可進入管理員面板。

或直接訪問：`http://localhost:8000/admin.html`

### 步驟3：使用管理功能

#### 🔑 修改API密鑰

1. 在管理員面板的"API配置管理"區域
2. 輸入新的API密鑰
3. 點擊"更新API密鑰"
4. 系統會自動更新 `config.py` 文件

#### 👥 管理用戶

1. 在"用戶管理"區域查看所有用戶
2. 使用搜索框搜索特定用戶
3. 點擊"查看"查看用戶詳細信息
4. 點擊"刪除"刪除用戶（不能刪除管理員）

#### 📊 系統統計

- 總用戶數
- 活躍用戶數
- 總練習數
- AI服務狀態

## 🔒 安全提示

1. **修改默認密碼**：首次登錄後請立即修改管理員密碼
2. **保護API密鑰**：API密鑰是敏感信息，請妥善保管
3. **備份數據**：定期使用"備份數據"功能導出數據
4. **謹慎操作**：刪除用戶和清空數據的操作無法撤銷

## 📝 修改管理員密碼

目前需要手動修改：

1. 打開瀏覽器開發者工具（F12）
2. 進入Console（控制台）
3. 運行以下代碼：

```javascript
// 獲取所有用戶
const users = JSON.parse(localStorage.getItem('sql-users') || '{}');

// 修改YIVH的密碼
if (users['YIVH']) {
    users['YIVH'].password = '您的新密碼';
    localStorage.setItem('sql-users', JSON.stringify(users));
    console.log('密碼已更新');
} else {
    console.log('管理員賬號不存在');
}
```

## 🛠️ 管理員功能詳解

### API配置管理

- **更新API密鑰**：直接更新後端配置文件
- **測試連接**：測試API密鑰是否有效
- **從配置文件讀取**：從後端讀取當前配置

### 用戶管理

- **搜索用戶**：按用戶名或電郵搜索
- **查看詳情**：查看用戶的學習統計
- **刪除用戶**：永久刪除用戶及其所有數據

### 系統設置

- **導出數據**：導出所有用戶數據為JSON文件
- **備份數據**：同導出功能
- **清空數據**：刪除所有數據（危險！）
- **刷新統計**：更新統計信息
- **查看日誌**：查看系統運行信息

## ⚠️ 注意事項

1. **數據存儲**：所有數據存儲在瀏覽器localStorage中
2. **跨設備**：不同設備/瀏覽器的數據不共享
3. **清除緩存**：清除瀏覽器數據會刪除所有用戶數據
4. **備份重要**：定期備份數據以防丟失

## 🔄 數據遷移

如果需要遷移數據到其他設備：

1. 在管理員面板點擊"導出所有數據"
2. 保存下載的JSON文件
3. 在新設備上打開管理員面板
4. 使用瀏覽器控制台運行：

```javascript
// 讀取導出的JSON文件內容（需要手動複製）
const backupData = { /* 粘貼導出的JSON內容 */ };

// 恢復數據
Object.keys(backupData).forEach(key => {
    if (key !== 'timestamp') {
        localStorage.setItem(key, JSON.stringify(backupData[key]));
    }
});
```

---

**管理員賬號已自動創建，現在就可以使用！** 🎉

