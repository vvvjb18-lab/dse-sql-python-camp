# DSE SQL 訓練營 - 技術架構說明

## 📐 整體架構

本項目採用 **三層架構**：

```
┌─────────────────────────────────────────────────────────┐
│                    前端層 (Frontend)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Web 前端    │  │  GUI 管理器   │  │  瀏覽器      │  │
│  │ (HTML/JS)    │  │ (Tkinter)    │  │  (用戶)      │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          │ HTTP/HTTPS       │ HTTP/HTTPS       │ HTTP/HTTPS
          │ RESTful API      │ RESTful API      │ RESTful API
          │                  │                  │
┌─────────┼──────────────────┼──────────────────┼──────────┐
│         ▼                  ▼                  ▼          │
│  ┌──────────────────────────────────────────────────┐   │
│  │           後端服務層 (Backend Service)            │   │
│  │              Flask Web Server                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │ REST API │  │ 靜態文件 │  │ AI 服務  │      │   │
│  │  └──────────┘  └──────────┘  └──────────┘      │   │
│  └──────────────────────────────────────────────────┘   │
│         │                  │                  │          │
└─────────┼──────────────────┼──────────────────┼──────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────┐
│                    數據層 (Data Layer)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ SQLite 數據庫│  │  文件系統    │  │  外部 API    │  │
│  │ (database.db)│  │ (HTML/JS)    │  │ (AI 服務)    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 技術棧

### 後端技術
- **語言**：Python 3.6+
- **Web 框架**：Flask 2.x
- **數據庫**：SQLite 3
- **AI 集成**：
  - 智譜AI (ZhipuAI SDK)
  - SiliconFlow API (DeepSeek/Qwen)
- **跨域支持**：Flask-CORS
- **環境變量**：python-dotenv

### GUI 管理工具
- **語言**：Python 3.6+
- **GUI 框架**：Tkinter (Python 標準庫)
- **系統調用**：subprocess
- **網絡請求**：urllib.request
- **權限管理**：pkexec (PolicyKit)

### 前端技術
- **HTML5** + **CSS3** (Tailwind CSS)
- **JavaScript** (ES6+)
- **可視化**：ECharts, Canvas API
- **代碼編輯**：CodeMirror
- **Markdown 渲染**：marked.js

---

## 🔄 GUI 與後端的交互方式

### 1. 服務控制交互

#### 啟動/停止服務
```python
# GUI 使用 subprocess 直接管理 Flask 進程
process = subprocess.Popen(
    ['python3', 'app.py'],
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    text=True
)
```

**特點**：
- **直接進程管理**：GUI 直接啟動/停止 Flask 進程
- **實時日誌捕獲**：通過 `stdout/stderr` 管道實時獲取日誌
- **進程監控**：定期檢查進程狀態（PID、端口監聽）

#### 健康檢查
```python
# GUI 通過 HTTP 請求檢查服務健康狀態
url = f"http://localhost:{self.port}/health"
req = urllib.request.Request(url)
response = urllib.request.urlopen(req, timeout=3)
```

**API 端點**：
- `GET /health` - 返回服務健康狀態

---

### 2. 數據獲取交互

#### 系統資源監控
```python
# GUI 直接讀取系統資源（無需後端）
cpu_usage = psutil.cpu_percent(interval=1)
mem_info = psutil.virtual_memory()
disk_info = psutil.disk_usage('/')
```

#### 訪問統計數據
```python
# GUI 通過 HTTP API 獲取訪問統計
url = f"http://localhost:{self.port}/api/stats/access"
req = urllib.request.Request(url)
response = urllib.request.urlopen(req, timeout=2)
data = json.loads(response.read().decode())
```

**API 端點**：
- `GET /api/stats/access` - 返回網站訪問統計（按小時聚合）

#### 用戶數據管理
```python
# GUI 直接讀取 SQLite 數據庫
conn = sqlite3.connect('database.db')
cursor = conn.cursor()
cursor.execute('SELECT * FROM users')
users = cursor.fetchall()
```

**特點**：
- **直接數據庫訪問**：GUI 可以直接讀取數據庫（本地文件）
- **無需 API 中間層**：減少網絡開銷
- **實時數據**：直接查詢，無延遲

---

### 3. 配置管理交互

#### API 密鑰配置
```python
# GUI 直接讀寫 config.py 文件
with open('config.py', 'r', encoding='utf-8') as f:
    content = f.read()
# 修改配置
with open('config.py', 'w', encoding='utf-8') as f:
    f.write(new_content)
```

**特點**：
- **文件直接操作**：GUI 直接修改配置文件
- **自動備份**：修改前自動備份原文件
- **即時生效**：需要重啟服務使配置生效

---

### 4. 外部服務交互

#### NPM Docker 容器管理
```python
# GUI 使用 pkexec 執行 sudo 命令
result = subprocess.run(
    ['pkexec', 'docker', 'start', 'npm'],
    capture_output=True,
    text=True
)
```

**特點**：
- **圖形化權限提升**：使用 `pkexec` 彈出密碼輸入窗口
- **Docker 命令執行**：直接調用 Docker CLI
- **狀態檢查**：定期檢查容器狀態

---

## 🌐 Web 前端與後端的交互方式

### 1. RESTful API 通信

#### AI 服務調用
```javascript
// 前端通過 fetch API 調用後端
const response = await fetch(`${AI_SERVICE_URL}/api/chat`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({
        message: userMessage,
        context: context,
        model: selectedModel
    })
});
```

**API 端點**：
- `POST /api/chat` - AI 聊天接口
- `POST /api/check-sql` - SQL 檢查接口
- `POST /api/explain` - 概念解釋接口

#### 用戶管理 API
```javascript
// 用戶註冊/登錄
const response = await fetch('/api/user/register', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username, email, password })
});
```

**API 端點**：
- `POST /api/user/register` - 用戶註冊
- `POST /api/user/login` - 用戶登錄
- `GET /api/user/profile` - 獲取用戶資料
- `PUT /api/user/profile` - 更新用戶資料
- `GET /api/user/progress` - 獲取學習進度
- `PUT /api/user/progress` - 更新學習進度

#### 學習進度 API
```javascript
// 保存學習進度
await fetch('/api/user/interactive-progress', {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ progress: progressData })
});
```

**API 端點**：
- `GET /api/user/interactive-progress` - 獲取互動練習進度
- `PUT /api/user/interactive-progress` - 保存互動練習進度

---

### 2. 靜態文件服務

```python
# Flask 直接提供靜態文件
@app.route('/')
def index():
    return send_file('index.html')

@app.route('/<path:filename>')
def serve_static(filename):
    return send_from_directory('.', filename)
```

**特點**：
- **直接文件服務**：Flask 直接提供 HTML/JS/CSS 文件
- **無需 Nginx**：開發環境可直接使用
- **生產環境**：可配合 NPM 反向代理使用

---

## 🔐 安全機制

### 1. 會話管理
```python
# Flask 使用 session 管理用戶登錄狀態
session['user_id'] = user_id
session['username'] = username
```

### 2. CORS 配置
```python
# 允許跨域請求（支持前端和後端分離部署）
CORS(app, supports_credentials=True)
```

### 3. 密碼加密
```python
# 使用 werkzeug 進行密碼哈希
from werkzeug.security import generate_password_hash, check_password_hash
hashed = generate_password_hash(password)
```

### 4. 權限管理
```python
# GUI 使用 pkexec 進行權限提升
subprocess.run(['pkexec', 'docker', '...'])
```

---

## 📊 數據流圖

### GUI → 後端服務
```
GUI (Tkinter)
    │
    ├─→ subprocess.Popen() → Flask 進程啟動
    │
    ├─→ urllib.request → HTTP API 調用
    │   └─→ /health, /api/stats/access
    │
    ├─→ sqlite3.connect() → 直接數據庫訪問
    │   └─→ 用戶數據、學習進度
    │
    └─→ file I/O → 配置文件讀寫
        └─→ config.py, domain_config.json
```

### Web 前端 → 後端服務
```
Web Browser
    │
    └─→ fetch() / XMLHttpRequest
        │
        ├─→ POST /api/chat → AI 服務
        ├─→ POST /api/user/login → 用戶認證
        ├─→ GET /api/user/progress → 學習進度
        └─→ PUT /api/user/interactive-progress → 保存進度
```

---

## 🎯 設計考量

### 1. 為什麼 GUI 直接管理進程？

**優點**：
- ✅ **即時控制**：無需額外的進程管理服務
- ✅ **資源效率**：減少中間層開銷
- ✅ **簡單可靠**：直接控制，減少故障點

**缺點**：
- ⚠️ **單機限制**：只能在本地管理
- ⚠️ **權限要求**：需要本地文件系統訪問權限

### 2. 為什麼 GUI 直接訪問數據庫？

**優點**：
- ✅ **性能優異**：無網絡延遲
- ✅ **實時數據**：直接查詢，無緩存問題
- ✅ **簡單直接**：無需額外的 API 層

**缺點**：
- ⚠️ **耦合度高**：GUI 需要了解數據庫結構
- ⚠️ **並發問題**：需要處理數據庫鎖定

### 3. 為什麼 Web 前端使用 RESTful API？

**優點**：
- ✅ **標準化**：遵循 REST 規範
- ✅ **跨平台**：支持多種前端框架
- ✅ **安全性**：通過 API 層進行權限控制
- ✅ **可擴展**：易於添加新功能

**缺點**：
- ⚠️ **網絡開銷**：每次請求都需要網絡通信
- ⚠️ **延遲**：相比直接數據庫訪問有延遲

---

## 🚀 性能優化

### 1. 異步操作
```python
# GUI 使用線程處理耗時操作
threading.Thread(target=long_running_task, daemon=True).start()
```

### 2. 緩存機制
```javascript
// 前端緩存 AI 模型選擇
localStorage.setItem('selectedModel', modelId);
```

### 3. 批量請求
```python
# 後端支持批量查詢
@app.route('/api/users/batch', methods=['POST'])
def get_users_batch():
    user_ids = request.json.get('user_ids', [])
    # 批量查詢用戶
```

### 4. 數據庫索引
```sql
-- 為常用查詢字段創建索引
CREATE INDEX idx_user_email ON users(email);
CREATE INDEX idx_progress_user_id ON user_progress(user_id);
```

---

## 🔄 擴展性考慮

### 1. 微服務架構（未來可選）

```
當前架構：
GUI → Flask (單體應用)

未來架構：
GUI → API Gateway → [Auth Service, AI Service, Data Service]
```

### 2. 數據庫遷移（未來可選）

```
當前：SQLite (單機)
未來：PostgreSQL/MySQL (分佈式)
```

### 3. 容器化部署（未來可選）

```dockerfile
# Dockerfile
FROM python:3.9
COPY . /app
RUN pip install -r requirements.txt
CMD ["python", "app.py"]
```

---

## 📝 總結

### 當前架構特點

1. **簡單高效**：GUI 直接管理進程和數據庫，減少中間層
2. **實時性強**：直接訪問數據，無網絡延遲
3. **易於維護**：單體應用，部署簡單
4. **功能完整**：涵蓋服務管理、數據監控、用戶管理

### 適用場景

- ✅ **單機部署**：適合單服務器環境
- ✅ **開發環境**：快速開發和測試
- ✅ **中小型項目**：用戶量適中的教育平台
- ✅ **內部工具**：管理後台和監控工具

### 未來優化方向

- 🔄 **分佈式架構**：支持多服務器部署
- 🔄 **API 統一化**：GUI 也通過 API 訪問數據
- 🔄 **容器化**：Docker 容器部署
- 🔄 **監控系統**：集成 Prometheus/Grafana

---

## 📚 相關文檔

- [GUI功能完整列表.md](./GUI功能完整列表.md) - GUI 功能詳解
- [服務管理器使用說明.md](./服務管理器使用說明.md) - 使用指南
- [NPM-Docker管理指南.md](./NPM-Docker管理指南.md) - NPM 管理說明

